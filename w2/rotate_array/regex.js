let re1 = /^([a, e, i, o, u]).*\1$/igm
// check if first and last characters are matching vowels 
// capture first characters that match from set, followed by any number with last char match value of capture

let re2 = /(Mr\.|Mrs\.|Ms\.|Dr\.|Er\.)[A-Za-z]+$/;
// match any of the listed options followed by characters (and not special characters)

let re3 = /([0-9]+)/gm
//return an array of all integers in a given string, excluding special characters

// reference >> https://www.hackerrank.com/challenges/js10-regexp-1/topics/javascript-regex

// future tutorials
